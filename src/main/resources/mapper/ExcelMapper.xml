<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sfdcupload.file.repository.FileRepository">

    <select id="totalAccFile" parameterType="String">
        select count(af.id)
        from mig.account_file af INNER JOIN mig.account_file_map afm ON af.account_number = afm.account_number
        where is_mig = false ;
    </select>

    <select id="findAccFile" parameterType="String">
        select afm.sfid as sfid, appnd_file_path as bbs_attach_file_name, af.id, is_mig, append_file
        FROM mig.account_file af INNER JOIN mig.account_file_map afm ON af.account_number = afm.account_number
        where is_mig = false
        order by length(append_file) asc
        limit 3;
    </select>

    <update id="updateAccFile" parameterType="java.util.Map">
        UPDATE mig.account_file
        SET is_mig =
        <foreach item="file" index="index" collection="listFile" separator=" " open="CASE id" close="END">
            WHEN #{file.id} THEN #{file.isMig}
        </foreach>
        WHERE id IN
        <foreach item="file" index="index" collection="listFile" separator="," open="(" close=")">
            #{file.id}
        </foreach>
    </update>

</mapper>

